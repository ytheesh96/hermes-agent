interface:
  display_name: "Loop Triage"
  short_description: "Build and evolve a live Loop task graph"
  default_prompt: "Use $loop-triage to build the smallest useful live task graph from brief titles and dependencies."
